version: 0.2

phases:
  install:
    commands:
      - echo Nothing to do in the install phase...
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mkdir build
      - cd build
      - cmake -G "Visual Studio 14 2015 Win64" ../
      - msbuild.exe RUN_TESTS.vcproj
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:

