sudo: false
language: c

osx_image: xcode8

#run builds for linux and osx. will add windows shortly
os:
  - osx
  - linux

compiler:
  - clang

#execute build
script:
  - mkdir build
  - cd build
  - cmake ..
  - make && make test

