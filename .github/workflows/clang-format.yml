name: Lint

on: [push]

jobs:
  clang-format:

    runs-on: ubuntu-24.04 # latest

    steps:
    - name: Checkout Sources
      uses: actions/checkout@v6

    - name: clang-format lint
      run: |
        ./format-check.py
